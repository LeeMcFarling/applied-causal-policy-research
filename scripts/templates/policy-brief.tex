% Policy Brief Template
% Requires: xelatex, fontspec (basic TeX Live)
% For full version: sudo tlmgr install titlesec mdframed footmisc booktabs caption setspace fancyhdr enumitem microtype
\documentclass[12pt, letterpaper]{article}

%% ── Packages (basic TeX Live safe) ──────────────────────────────────────────
\usepackage{fontspec}
\usepackage{polyglossia}
\setdefaultlanguage{english}
\usepackage[margin=1.25in, top=1.1in, bottom=1.1in]{geometry}
\usepackage[hidelinks]{hyperref}
\usepackage{graphicx}
\newcommand*\pandocbounded[1]{%
  \begingroup
  \setbox0=\hbox{#1}%
  \ifdim\wd0>\linewidth
    \resizebox{\linewidth}{!}{\box0}%
  \else
    \box0
  \fi
  \endgroup
}
\usepackage{longtable}
\usepackage{array}
\usepackage{xcolor}
\usepackage{amsmath}
\usepackage{calc}
\providecommand{\real}[1]{#1}

%% ── Optional packages (install with: sudo tlmgr install <name>) ─────────────
\IfFileExists{microtype.sty}{\usepackage{microtype}}{}
\IfFileExists{setspace.sty}{\usepackage{setspace}\setstretch{1.18}}{}
\IfFileExists{fancyhdr.sty}{
  \usepackage{fancyhdr}
  \pagestyle{fancy}
  \fancyhf{}
  \fancyhead[L]{\small\color{policy-meta}\textsf{$if(domain)$$domain$$else$Policy Brief$endif$}}
  \fancyhead[R]{\small\color{policy-meta}\textsf{$if(status)$$status$$endif$$if(phase)$ · Phase $phase$$endif$}}
  \fancyfoot[C]{\small\color{policy-meta}\thepage}
  \renewcommand{\headrulewidth}{0.4pt}
  \renewcommand{\headrule}{\color{policy-rule}\hrule width\headwidth height\headrulewidth}
  \fancypagestyle{firstpage}{\fancyhf{}\fancyfoot[C]{\small\color{policy-meta}\thepage}\renewcommand{\headrulewidth}{0pt}}
}{\pagestyle{plain}}
\IfFileExists{titlesec.sty}{
  \usepackage{titlesec}
  \titleformat{\section}{\large\bfseries\color{policy-navy}}{}{0em}{}[\vspace{-4pt}\color{policy-rule}\rule{\linewidth}{0.5pt}\vspace{2pt}]
  \titleformat{\subsection}{\normalsize\bfseries\color{policy-navy}}{}{0em}{}
  \titleformat{\subsubsection}{\normalsize\bfseries}{}{0em}{}
  \titlespacing*{\section}{0pt}{18pt}{6pt}
  \titlespacing*{\subsection}{0pt}{12pt}{3pt}
}{}
\IfFileExists{footmisc.sty}{\usepackage[hang,flushmargin,multiple]{footmisc}}{}
\IfFileExists{booktabs.sty}{\usepackage{booktabs}}{}
\IfFileExists{enumitem.sty}{\usepackage{enumitem}\setlist[itemize]{leftmargin=1.5em,itemsep=2pt,topsep=4pt}\setlist[enumerate]{leftmargin=1.5em,itemsep=2pt,topsep=4pt}}{}
\IfFileExists{caption.sty}{\usepackage{caption}\captionsetup{font=small,labelfont=bf,labelsep=period}}{}
\IfFileExists{mdframed.sty}{\usepackage{mdframed}}{}

%% ── Fonts ────────────────────────────────────────────────────────────────────
\setmainfont{Georgia}[Ligatures=TeX]
\setsansfont[Scale=0.92]{Helvetica Neue}[Ligatures=TeX]
\setmonofont[Scale=0.85]{Courier New}

%% ── Colors ───────────────────────────────────────────────────────────────────
\definecolor{policy-navy}{RGB}{15, 52, 96}
\definecolor{policy-rule}{RGB}{200, 200, 200}
\definecolor{policy-meta}{RGB}{100, 100, 100}

%% ── Base spacing ─────────────────────────────────────────────────────────────
\setlength{\parskip}{6pt}
\setlength{\parindent}{0pt}
\setlength{\footnotesep}{8pt}

%% ── Footnote rule (Chicago style) ────────────────────────────────────────────
\renewcommand{\footnoterule}{%
  \kern-3pt
  \hrule width 2in height 0.4pt
  \kern2.6pt
}

%% ── Tables ───────────────────────────────────────────────────────────────────
\renewcommand{\arraystretch}{1.3}
\setlength{\tabcolsep}{8pt}

%% ── Figures ──────────────────────────────────────────────────────────────────
\setkeys{Gin}{width=\linewidth, keepaspectratio}

%% ── Code blocks ──────────────────────────────────────────────────────────────
$if(highlighting-macros)$
$highlighting-macros$
$endif$

%% ─────────────────────────────────────────────────────────────────────────────
\begin{document}

%% ── Cover block ──────────────────────────────────────────────────────────────
\IfFileExists{fancyhdr.sty}{\thispagestyle{firstpage}}{\thispagestyle{empty}}

\vspace*{0.2in}
{\color{policy-navy}\rule{\linewidth}{2pt}}
\vspace{8pt}

{\fontsize{22}{26}\selectfont\bfseries\color{policy-navy}
$if(title)$$title$$else$Untitled Brief$endif$}

\vspace{10pt}
{\color{policy-rule}\rule{\linewidth}{0.5pt}}
\vspace{6pt}

\begin{minipage}[t]{0.6\linewidth}
$if(domain)${\small\textsf{\color{policy-meta}Domain: \textbf{$domain$}}}\\[2pt]$endif$
$if(author)${\small\textsf{\color{policy-meta}Author: $author$}}\\[2pt]$endif$
\end{minipage}
\begin{minipage}[t]{0.38\linewidth}
\raggedleft
$if(version)${\small\textsf{\color{policy-meta}Version: $version$}}\\[2pt]$endif$
$if(date)${\small\textsf{\color{policy-meta}$date$}}\\[2pt]$endif$
$if(phase)${\small\textsf{\color{policy-meta}Phase $phase$}}\\[2pt]$endif$
\end{minipage}

$if(description)$
\vspace{14pt}
\noindent\colorbox{policy-navy!6}{%
  \begin{minipage}{\dimexpr\linewidth-2\fboxsep}
    \vspace{8pt}
    \small $description$
    \vspace{8pt}
  \end{minipage}%
}
$endif$

\vspace{16pt}
{\color{policy-rule}\rule{\linewidth}{0.5pt}}
\vspace{14pt}

%% ── Body ─────────────────────────────────────────────────────────────────────
$body$

\end{document}
